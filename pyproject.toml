[project]
name = "word2vec-numpy"
version = "0.1.0"
description = "word2vec with pandas and numpy"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "matplotlib>=3.10.8",
    "numpy>=2.4.3",
    "tqdm>=4.67.3",
]
